// Loader Component
// This component represents a spinning loader, typically used to indicate that content is being loaded or processed.
const Loader = () => {
  return (
    // Spinner element with animation classes for spinning effect
    <div className="animate-spin rounded-full h-16 w-16 border-t-4 border-pink-500 border-opacity-50"></div>
  );
};

export default Loader;
